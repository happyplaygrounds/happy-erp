

<h1>Listing Orders</h1>



<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Number</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @happyorder.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.number %></td>
        <td><%#= link_to "Show", book %></td>
        <td><%#= link_to "Edit", edit_book_path(book) %></td>
        <td><%#= link_to "Destroy", book, method: :delete, data: { confirm: "Are you sure?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
